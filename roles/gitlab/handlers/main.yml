- name: reconfigure gitlab
  shell: docker exec gitlab gitlab-ctl reconfigure
  listen:
    - "reload gitlab"

- name: restart gitlab
  shell: docker exec gitlab gitlab-ctl restart
  listen:
    - "reload gitlab"
