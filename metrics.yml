- name: configure logging
  hosts: all
  roles:
    - docker
    - rsyslog

- name: spin up elasticsearch
  hosts: es
  roles:
    - elasticsearch

- name: spin up everything else for metrics
  hosts: data
  roles:
    - logstash
    - kibana
    - prometheus
    - curator
